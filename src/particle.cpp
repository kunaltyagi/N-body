#include <particle.h>

Particle::Particle()
{
    x = random(200) - 400;
    y = random(200) - 400;
    z = random(200) - 400;
    vx = random(20) - 40;
    vy = random(20) - 40;
    vz = random(20) - 40;
    mass   = random(300);
    radius = k*
}
